<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CommonDAO">

    <!-- 프로젝트 리스트 조회 -->
    <select id="selectMenuList" resultType="hashMap" parameterType="hashMap">
        SELECT 'prj' as gbn, prj_nm AS nm, listImgId , mainImgId, disp_yn as dispYn
        FROM tb_prj_mst
        WHERE exps_yn = 'Y'
        AND del_yn = 'N'
    </select>
    
    <!-- 공지사항 등록 -->
    <insert id="insertAdminlog" parameterType="hashMap">
        /*** CommonDAO.insertAdminlog ***/
        INSERT INTO tb_systemlog 
               (
                inqire_dt  /*** 로그 등록일시***/
                , srvc_nm  /*** 로그 실행 명칭***/
                , inqire_info  /*** 로그 싱행 실행 클래스***/
                , unique_id  /*** 등록자 순번***/
                , rqester_ip  /*** 로그 ip***/
               )
        VALUES
               (
                 NOW()   /*** 로그 등록일시***/
                , #{srvcNm}  /*** 로그 실행 명칭***/
                ,#{ inqireInfo}  /*** 로그 싱행 실행 클래스***/
                , #{uniqueId}  /*** 등록자 순번***/
                , #{rqesterIp}  /*** 로그 ip***/
                )
    </insert>
    
</mapper>